//function sum(x, y) {
//  return x + y;
//}
//console.log(sum(1, 2));

//exemple arrow function
const sum = (x, y) => {
    let result = x + y;
    return result;
}

const multiply = (x, y) => x * y;

console.log(sum(1, 2));
console.log(multiply(2, 3));
